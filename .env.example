# Nuvem (Supabase) — opcional: sem estas variáveis o app roda 100% offline.
# Use APENAS a chave pública (anon); a segurança por usuário é feita por
# Row Level Security no banco (ver docs/SUPABASE.md).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
